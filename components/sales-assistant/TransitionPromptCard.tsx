import { AssistantIcon } from "./AssistantIcon";

export function TransitionPromptCard({ isArabic, onConfirm, loading = false }: { isArabic: boolean; onConfirm: () => void; loading?: boolean }) {
  return <section data-testid="transition-prompt-card" className="rounded-2xl border border-emerald-300/15 bg-gradient-to-br from-emerald-300/[0.075] via-sky-300/[0.035] to-transparent p-4 shadow-[0_18px_50px_-38px_rgba(52,211,153,.5)]">
    <div className="flex items-start gap-3"><span className="flex h-9 w-9 shrink-0 items-center justify-center rounded-xl bg-emerald-300/10 text-emerald-200"><AssistantIcon name="sparkles" className="h-5 w-5" /></span><div className="min-w-0"><h3 className="text-sm font-semibold text-slate-100">{isArabic ? "الحل جاهز للخطوة التجارية" : "Ready for the commercial step"}</h3><p className="mt-1 text-xs leading-5 text-slate-400">{isArabic ? "تقدر تضيف أو تغيّر أي شيء، أو تبدأ تجهيز العرض من المعلومات المؤكدة." : "You can add or change anything, or prepare the quotation from confirmed information."}</p><button type="button" onClick={onConfirm} disabled={loading} className="mt-3 inline-flex items-center gap-1.5 rounded-xl bg-emerald-300 px-3 py-2 text-xs font-bold text-slate-950 outline-none hover:bg-emerald-200 focus-visible:ring-2 focus-visible:ring-emerald-300 focus-visible:ring-offset-2 focus-visible:ring-offset-slate-950 disabled:cursor-wait disabled:opacity-60">{loading ? (isArabic ? "جارٍ تجهيز العرض…" : "Preparing quotation…") : (isArabic ? "جهّز العرض" : "Prepare quotation")}<AssistantIcon name="chevron" className={`h-3.5 w-3.5 ${isArabic ? "rotate-180" : ""}`} /></button></div></div>
  </section>;
}
