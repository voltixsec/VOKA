"use client";
export function VoiceOrb({ state = "IDLE", size = "compact", disabled = false, label, title, onClick }: { state?: "IDLE" | "LISTENING" | "PROCESSING"; size?: "hero" | "compact"; disabled?: boolean; label: string; title?: string; onClick?: () => void }) { const dimension = size === "hero" ? "h-56 w-56" : "h-12 w-12"; return <button type="button" aria-label={label} title={title} aria-pressed={state === "LISTENING"} disabled={disabled || !onClick} onClick={onClick} className={`group relative grid ${dimension} shrink-0 place-items-center rounded-full disabled:cursor-default`}><span aria-hidden="true" className={`absolute inset-[8%] rounded-full border border-sky-300/30 ${state === "LISTENING" ? "motion-safe:animate-ping" : "motion-safe:animate-[pulse_4s_ease-in-out_infinite]"}`} /><span aria-hidden="true" className="absolute inset-[18%] rounded-full bg-sky-500/25 blur-xl transition group-hover:bg-sky-400/35" /><span aria-hidden="true" className="absolute inset-[25%] rounded-full border border-white/25 bg-gradient-to-br from-cyan-200 via-sky-500 to-violet-600 shadow-[0_0_45px_rgba(56,189,248,.55)]" /><span className="sr-only">{label}</span>{state === "LISTENING" ? <span aria-hidden="true" className="absolute -bottom-1 flex h-3 items-end gap-0.5">{[6, 11, 8, 12, 7].map((height, index) => <i key={index} className="w-0.5 rounded-full bg-cyan-200 motion-safe:animate-pulse" style={{ height }} />)}</span> : null}</button>; }
