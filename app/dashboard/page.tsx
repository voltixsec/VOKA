// The dashboard home route renders the live Command Center. The previous
// LegacyDashboardPage implementation was dead code (never the default export)
// and is intentionally removed to keep a single source of truth for the
// dashboard home experience.
export { DashboardCommandCenter as default } from "@/components/dashboard/command-center/DashboardCommandCenter";
