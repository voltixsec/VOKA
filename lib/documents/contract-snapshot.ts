import { ApiError } from '@/lib/api'; import { prisma } from '@/lib/prisma'; import { GetContractUseCase } from '@/src/application/contract'; import { PrismaContractRepository } from '@/src/infrastructure/persistence/prisma/contract/PrismaContractRepository'; import { serializeContract } from '@/app/api/contracts/serialize-contract';
const getContract = new GetContractUseCase(new PrismaContractRepository(prisma));
export function contractIdFromDocumentRequest(request:Request,format:'pdf'|'xlsx'){const parts=new URL(request.url).pathname.split('/').filter(Boolean);const index=parts.indexOf(format);if(index<1)throw ApiError.badRequest('CONTRACT_ID_REQUIRED','contractId is required.');return decodeURIComponent(parts[index-1]);}
export async function getContractDocumentSnapshot(companyId:string,contractId:string){const contract=await getContract.execute(companyId,contractId);if(!contract)throw ApiError.notFound('CONTRACT_NOT_FOUND','Contract not found.');return serializeContract(contract);}
