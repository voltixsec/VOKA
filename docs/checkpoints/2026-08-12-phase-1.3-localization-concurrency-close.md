# VOKA — Phase 1.3 Localization Concurrency Close

Date: 2026-08-12  
Project: VOKA – AI Sales OS  
Branch: `feature/localization-status`  
Final HEAD: `6ff6777`

## Phase objective

Phase 1.3 closes quotation localization retry and concurrency safety. Localization remains asynchronous, persisted, bounded, and protected against concurrent or stale workers.

## Completed scope

- deterministic source-generation signature tracking
- atomic localization claim
- claim-token fencing
- 12-minute claim lease
- persisted attempt count
- maximum of three actual execution attempts
- retryable failures:
  - `TRANSLATION_TIMEOUT`
  - `TRANSLATION_PROVIDER_ERROR`
  - `TRANSLATION_UNEXPECTED_ERROR`
- non-retryable failure:
  - `TRANSLATION_INVALID_RESPONSE`
- fenced completion and fenced failure
- narrow localization-only persistence
- quotation-line updates by explicit ID only
- no positional or array-index fallback
- no generic quotation update in the AI persistence path
- reusable `QuotationLocalizationJobRunner`
- persisted recovery discovery
- bounded recovery batch size of 10
- sequential recovery execution
- protected recovery endpoint
- `VOKA_CRON_SECRET` deployment requirement
- atomic claim remains the final execution authority

Persistent database statuses remain only:

- `PENDING`
- `COMPLETED`
- `FAILED`

## Runtime invariant

```text
Save
→ persist commercial/user data immediately
→ return HTTP success
→ background localization
→ atomic claim
→ re-read current database state
→ validate source signature
→ run AI localization
→ fenced completion or failure
```

AI failure must never turn an already successful Save into HTTP 500.

## Recovery invariant

Recovery discovery is only a bounded prefilter. `QuotationLocalizationJobRunner` must still acquire the atomic claim before AI execution. Two recovery invocations may safely discover the same row; only the worker that acquires the claim can continue and finalize through the signature-and-token fence.

Recovery does not manually clear expired claims, bypass the runner, or duplicate localization execution logic.

## Key commits

Phase 1.2 lifecycle foundation:

- `59f45c5` — localization lifecycle core
- `7e5ac03` — lifecycle persistence
- `b882870` — deterministic lifecycle backfill
- `9a1403e` — lifecycle migration finalization
- `790d324` — Prisma shadow URL and Node 22 CI configuration

Phase 1.3:

- `df0f997` — concurrency metadata
- `c4997d1` — atomic claim
- `aa4e85b` — source generations
- `0cf7b7a` — fenced completion and failure
- `2bc8f81` — job runner extraction
- `6ff6777` — persisted recovery and retry trigger

## Deployment requirement

Configure `VOKA_CRON_SECRET` in the deployment environment and schedule the protected localization-recovery endpoint. Do not commit, document, or log an actual secret value.

## Explicit non-goals and deferred work

- no Redis, BullMQ, RabbitMQ, or Kafka
- no external queue
- no infinite or recursive retry
- no new persistent localization statuses
- no exponential-backoff schema
- no parallel batch AI execution

## Validation evidence

- Step 5 final: 7 files, 58 tests passed
- Step 6A final: 7 files, 59 tests passed
- Step 6B final: 6 files, 36 tests passed
- TypeScript typecheck: PASS at each closure
- diff hygiene: PASS at each closure

## Branch and stash checkpoint

Local branches:

- `main`
- `feature/localization-status`

Remote branches:

- `main`
- `feature/localization-status`
- `feature/changed-fields-only-localization`

Retained PR/base branch:

- `feature/changed-fields-only-localization`

Retained stashes — do not alter or drop:

- `stash@{0}: wip/customers-before-sprint-10a-close`
- `stash@{1}: GitHub Desktop mvp-01-foundation stash`

## CTO close

Phase 1.3 — Quotation Localization Retry + Concurrency Safety — is **CLOSED**.

Do not begin the next feature from this checkpoint. CTO progress review should determine the next roadmap step.
